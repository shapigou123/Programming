#include <stdio.h>
#include <string.h>
int main()
{
	int num = 0;
	char str[100];
	char str2[100];
	scanf("%s",str);
	printf("%s\n",str);
	putchar('c');
	puts("hello puts\n");
	//gets(str2);
	//puts(str2);
	return 0;
}
